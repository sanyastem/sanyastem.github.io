---
layout: post
title: "Claude Code: Skills — собственные команды для агента"
categories: tools
date: 2026-03-02
date_ru: "2 марта 2026"
read_time: 5
difficulty: intermediate
series: "Claude Code: полный гайд"
part: 2
description: "Что такое Skills в Claude Code, как писать собственные команды-скрипты и вызывать их через /команда прямо в сессии."
excerpt_text: "Skills — это собственные /команды для Claude Code. Пишешь один раз, вызываешь в любой сессии"
keywords: "claude code skills, slash commands claude, кастомные команды claude code, .claude/commands"
---

## Что такое Skills

Skills (навыки) — это кастомные команды для Claude Code, которые вызываются через `/имя` прямо в сессии. По сути, это Markdown-файлы с инструкцией: что сделать, когда пользователь вызвал команду.

Вместо того чтобы каждый раз объяснять агенту «сделай код-ревью по нашим стандартам», ты один раз пишешь skill и дальше просто набираешь `/review`.

## Где хранятся Skills

```
.claude/
└── commands/
    ├── review.md        # /review
    ├── commit.md        # /commit
    └── deploy-check.md  # /deploy-check
```

Глобальные skills (для всех проектов):

```
~/.claude/
└── commands/
    └── standup.md       # /standup — везде
```

## Структура файла

Минимальный skill — это просто Markdown:

```markdown
# Review

Сделай код-ревью всех изменённых файлов.
Проверь: нет ли багов, соблюдены ли наши соглашения из CLAUDE.md,
нет ли явных проблем с производительностью.
Оформи как список замечаний с указанием файла и строки.
```

Сохрани как `.claude/commands/review.md` — и теперь в сессии доступна команда `/review`.

## Пример: skill для коммитов

```markdown
# Commit

Создай git-коммит для всех staged изменений.

1. Запусти `git diff --staged` чтобы увидеть что меняется
2. Придумай сообщение в формате Conventional Commits:
   - feat: новая функция
   - fix: исправление бага
   - refactor: рефакторинг без изменения поведения
   - docs: только документация
3. Запусти `git commit -m "сообщение"`
4. Покажи итоговый коммит через `git log -1`
```

## Пример: skill для проверки перед деплоем

```markdown
# Deploy check

Перед деплоем проверь:

1. `npm run lint` — нет ошибок линтера
2. `npm run test` — все тесты зелёные
3. `npm run build` — сборка проходит
4. Нет незакоммиченных изменений (`git status`)

Если что-то упало — останови и сообщи что именно.
Если всё ок — напиши "Готово к деплою ✓"
```

## Аргументы в Skills

Skills могут принимать аргументы через `$ARGUMENTS`:

```markdown
# Fix issue

Посмотри на GitHub Issue #$ARGUMENTS.
Найди связанный код в репозитории и предложи исправление.
```

Вызов: `/fix-issue 142`

## Пример: skill для дейлистендапа

```markdown
# Standup

Помоги написать сообщение для дейлистендапа.

1. Запусти `git log --since="yesterday" --oneline --author="$(git config user.name)"`
2. На основе коммитов сформируй текст:
   - **Вчера:** что сделал (из коммитов)
   - **Сегодня:** что планирую (спроси у меня)
   - **Блокеры:** есть ли что-то что мешает

Формат — короткие пункты, без лишней воды.
```

## Встроенные Skills

В Claude Code есть встроенные команды:

| Команда | Что делает |
|---------|-----------|
| `/help` | Список всех команд |
| `/clear` | Сбросить контекст сессии |
| `/compact` | Сжать историю диалога |
| `/memory` | Управление долгосрочной памятью |
| `/doctor` | Диагностика установки |
| `/bug` | Сообщить о баге в Claude Code |

## Итого

- Файлы `.claude/commands/*.md` → команды `/имя` в сессии
- Глобальные skills в `~/.claude/commands/` работают во всех проектах
- `$ARGUMENTS` — для передачи параметров
- Описывай инструкцию подробно — агент выполняет её буквально
