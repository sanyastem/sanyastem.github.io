---
layout: post
title: "Claude Code: Skills — собственные команды для агента"
categories: ai
translation_of: "/en/ai/claude-code-skills/"
tldr:
  - "Skills — Markdown-инструкции для Claude Code: файл .claude/skills/<имя>/SKILL.md создаёт /команду; старый формат .claude/commands/*.md работает для совместимости."
  - "Один SKILL.md даёт и ручную /команду, и авто-вызов агентом по description; поведение управляется frontmatter: user-invocable, disable-model-invocation."
  - "Frontmatter поддерживает allowed-tools (ограничение инструментов), argument-hint, effort (low/medium/high), context: fork — запуск в субагенте; аргументы — через $ARGUMENTS."
  - "Шарить skills между проектами — через плагины: /plugin marketplace add owner/repo, затем /plugin install имя@маркетплейс; вызов с префиксом /плагин:скилл."
date: 2026-03-02
last_modified_at: 2026-05-24
date_ru: "2 марта 2026"
read_time: 5
difficulty: intermediate
series: "Claude Code: полный гайд"
part: 2
description: "Что такое Skills в Claude Code, как писать собственные команды-скрипты и вызывать их через /команда прямо в сессии."
excerpt_text: "Skills — это собственные /команды для Claude Code. Пишешь один раз, вызываешь в любой сессии"
keywords: "claude code skills, slash commands claude, кастомные команды claude code, .claude/commands"
faq:
  - q: "Skills vs slash-команды — это разное?"
    a: "В 2026 их объединили — файлы в .claude/commands/*.md и .claude/skills/<name>/SKILL.md создают одинаковую /команду. Разница в том кто вызывает: slash-команда запускается тобой вручную, agent skill Claude может запустить сам когда задача подходит под description. Один SKILL.md даёт оба способа."
  - q: "Skills vs Subagents — в чём разница?"
    a: "Skill — это шаблон промпта, который Claude выполняет в текущей сессии (видит весь контекст). Subagent — изолированная под-сессия со своим контекстом (не видит главную). Skills для рутинных задач, subagents для тяжёлых параллельных или для защиты главного контекста от шума."
  - q: "Где хранить общие для всех проектов skills?"
    a: "В ~/.claude/skills/ — глобальные, доступны во всех проектах. В <project>/.claude/skills/ — локальные, только в этом проекте. Локальные перебивают глобальные при совпадении имени. Команду удобно хранить там где она применяется чаще."
  - q: "Можно ли вызывать skill из другого skill?"
    a: "Да, через /command в инструкции. Например /commit может включить «после написания запусти /lint». Но цепочки длиннее 2-3 редко окупаются — проще написать один большой skill с разветвлённой логикой."
  - q: "Skills и MCP вместе работают?"
    a: "Да, отлично. Skill — это «как делать», MCP — «куда обращаться». Skill /create-jira-ticket использует MCP-сервер Jira чтобы создать тикет, плюс свою логику форматирования. Часто комбинация даёт лучше чем каждое по отдельности."
---

## Что такое Skills

Skills (навыки) — это Markdown-файлы с инструкцией для агента. Пишешь один раз, дальше используешь в любой сессии. Вместо того чтобы каждый раз объяснять «сделай код-ревью по нашим стандартам» — один раз пишешь skill и набираешь `/review`. Если Claude Code ещё не установлен — начни с [установки и базовой настройки](/ai/claude-code-setup/).

## Skills и slash-команды — теперь одно

В 2026 Anthropic влила старые кастомные команды в Skills — теперь это **одна система**, а Skills стали основным, более функциональным форматом:

- **Slash-команды** (старый формат `.claude/commands/*.md`) — вызываешь вручную через `/имя`, работают ради обратной совместимости
- **Agent Skills** (основной формат `.claude/skills/<name>/SKILL.md`) — то же `/имя`, плюс Claude может вызвать **сам** по `description`; поддерживают frontmatter и вспомогательные файлы рядом

Файлы в обеих папках создают `/команду`. Разница в том **кто вызывает**:

| | Кто запускает | Когда использовать |
|---|---|---|
| **User-invoked** | Ты вручную `/deploy` | Действия с побочным эффектом, которые ты контролируешь |
| **Agent-invoked** | Claude сам по `description` | Навыки которые модель применяет автоматически (форматирование, проверки) |

Один `SKILL.md` даёт **оба**: и авто-триггер по описанию, и ручную `/команду`. Управляешь поведением через frontmatter (`user-invocable`, `disable-model-invocation`).

## Где хранятся Skills

Рекомендуемый формат — папка с `SKILL.md`:

```
.claude/
└── skills/
    ├── review/
    │   └── SKILL.md     # /review
    ├── commit/
    │   └── SKILL.md     # /commit
    └── deploy-check/
        └── SKILL.md     # /deploy-check
```

Старый формат `.claude/commands/*.md` тоже работает (обратная совместимость), но новые skills лучше класть в `.claude/skills/` — он поддерживает frontmatter, вспомогательные файлы рядом и авто-вызов агентом.

Глобальные skills (для всех проектов):

```
~/.claude/
└── skills/
    └── standup/
        └── SKILL.md     # /standup — везде
```

## Структура файла — SKILL.md с frontmatter

```markdown
---
name: review
description: Код-ревью изменений по стандартам проекта
allowed-tools: Read, Bash
argument-hint: "[путь к файлу или пусто для всех изменений]"
effort: medium
---

Сделай код-ревью всех изменённых файлов.
Проверь: нет ли багов, соблюдены ли наши соглашения из CLAUDE.md,
нет ли явных проблем с производительностью.
Оформи как список замечаний с указанием файла и строки.
```

Сохрани как `.claude/skills/review/SKILL.md` — и теперь в сессии доступна команда `/review`.

**Ключевые поля frontmatter:**

| Поле | Что делает |
|------|-----------|
| `description` | Описание (до 1024 символов) — Claude понимает когда применять |
| `allowed-tools` | Ограничить инструменты (Read, Edit, Write, Bash...) |
| `argument-hint` | Подсказка какие аргументы принимает |
| `effort` | `low` / `medium` / `high` — глубина проработки (больше = больше токенов) |
| `context: fork` | Запустить в субагенте (изоляция) |
| `user-invocable: false` | Только Claude может вызывать, не пользователь |
| `disable-model-invocation: true` | Только вручную через `/имя`, Claude сам не запускает |

## Пример: skill для коммитов

```markdown
# Commit

Создай git-коммит для всех staged изменений.

1. Запусти `git diff --staged` чтобы увидеть что меняется
2. Придумай сообщение в формате Conventional Commits:
   - feat: новая функция
   - fix: исправление бага
   - refactor: рефакторинг без изменения поведения
   - docs: только документация
3. Запусти `git commit -m "сообщение"`
4. Покажи итоговый коммит через `git log -1`
```

## Пример: skill для проверки перед деплоем

```markdown
# Deploy check

Перед деплоем проверь:

1. `npm run lint` — нет ошибок линтера
2. `npm run test` — все тесты зелёные
3. `npm run build` — сборка проходит
4. Нет незакоммиченных изменений (`git status`)

Если что-то упало — останови и сообщи что именно.
Если всё ок — напиши "Готово к деплою ✓"
```

## Аргументы в Skills

Skills могут принимать аргументы через `$ARGUMENTS`:

```markdown
# Fix issue

Посмотри на GitHub Issue #$ARGUMENTS.
Найди связанный код в репозитории и предложи исправление.
```

Вызов: `/fix-issue 142`

## Пример: skill для дейлистендапа

```markdown
# Standup

Помоги написать сообщение для дейлистендапа.

1. Запусти `git log --since="yesterday" --oneline --author="$(git config user.name)"`
2. На основе коммитов сформируй текст:
   - **Вчера:** что сделал (из коммитов)
   - **Сегодня:** что планирую (спроси у меня)
   - **Блокеры:** есть ли что-то что мешает

Формат — короткие пункты, без лишней воды.
```

## Встроенные Skills

В Claude Code есть встроенные команды и bundled skills:

| Команда | Что делает |
|---------|-----------|
| `/help` | Список всех команд |
| `/clear` | Сбросить контекст сессии |
| `/compact` | Сжать историю диалога |
| `/memory` | Управление CLAUDE.md файлами памяти |
| `/doctor` | Диагностика установки |
| `/rewind` | Откат к предыдущему checkpoint |
| `/batch` | Параллельные изменения в большой кодовой базе |
| `/loop` | Повторять задачу по расписанию (`/loop 5m /check`) |
| `/code-review` | Ревью изменений на баги и качество |
| `/security-review` | Поиск уязвимостей в изменениях |
| `/verify` | Запустить приложение и проверить, что правка работает |
| `/run` | Запустить приложение проекта |

## Плагины и маркетплейсы

Чтобы не копировать `.claude/skills/` вручную между проектами и командами, skills (а также команды, агенты, hooks и MCP-серверы) распространяют через **плагины** из маркетплейсов:

```bash
# Подключить маркетплейс
/plugin marketplace add owner/repo

# Установить плагин (со скиллами/командами внутри)
/plugin install имя-плагина@маркетплейс
```

Официальный маркетплейс (`claude-plugins-official`) доступен сразу. Скиллы из плагина вызываются с префиксом: `/имя-плагина:имя-скилла`.

Готовые подборки скиллов под конкретный стек — в статьях [Топ Skills для .NET + Angular + Docker](/ai/claude-code-skills-dotnet/) и [Skills для миграции](/ai/claude-code-skills-migration/).

## Итого

- Основной формат: `.claude/skills/<name>/SKILL.md` с frontmatter; `.claude/commands/*.md` — для обратной совместимости
- `$ARGUMENTS` — для передачи параметров
- `allowed-tools` в frontmatter — ограничивает что агент может делать
- `effort: high` — для сложных задач которые требуют глубокого анализа
- Делиться скиллами между проектами — через плагины (`/plugin install`)
